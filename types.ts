// ไฟล์นี้ใช้สำหรับสร้างชนิดข้อมูลใหม่ที่ล้อกับฐานข้อมูล
export type CoffeeShop = {
  id: string;
  name: string;
  district: string;
  description: string;
  image_url: string;
  phone: string;
  latitude: number;
  longitude: number;
};
